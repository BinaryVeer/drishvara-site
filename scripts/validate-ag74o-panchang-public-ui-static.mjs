import fs from "node:fs";
import crypto from "node:crypto";
const fail=(m)=>{console.error("❌ AG74O-R3 static validation failed: "+m);process.exit(1);};
const pass=(m)=>console.log("✅ "+m);
const read=(p)=>fs.readFileSync(p,"utf8");
const json=(p)=>JSON.parse(read(p));
const required=[
"index.html","package.json","assets/vendor/astronomy-engine-2.1.19.min.js","assets/vendor/astronomy-engine-2.1.19.LICENSE.txt","assets/js/ag74o-panchang-public-controller.js","scripts/ag74o-panchang-browser-qa.html","scripts/run-ag74o-panchang-browser-qa.sh","data/knowledge-base/panchang-festival/production/ag74o-panchang-public-runtime-contract.json","data/knowledge-base/location-intelligence/production/ag74o-r2-browser-approved-location-projection.json","data/knowledge-base/panchang-festival/production/ag74o-r3-calendar-activation-projection.json","data/knowledge-base/panchang-festival/production/ag74o-r3-festival-observance-activation-projection.json","data/knowledge-base/panchang-festival/production/ag74o-r3-request-commit-flow-contract.json"];
for(const p of required)if(!fs.existsSync(p))fail("Missing "+p);
const index=read("index.html"),controller=read("assets/js/ag74o-panchang-public-controller.js"),pkg=json("package.json"),runtime=json("data/knowledge-base/panchang-festival/production/ag74o-panchang-public-runtime-contract.json"),locations=json("data/knowledge-base/location-intelligence/production/ag74o-r2-browser-approved-location-projection.json"),calendar=json("data/knowledge-base/panchang-festival/production/ag74o-r3-calendar-activation-projection.json"),festival=json("data/knowledge-base/panchang-festival/production/ag74o-r3-festival-observance-activation-projection.json");
for(const marker of ["AG74O_PANCHANG_PUBLIC_UI_STYLE_START",'data-ag74o-r2-approved-option-count="0"','data-ag74o-r2-ui-state-only="true"','id="panchang-location-provenance"','id="panchang-calculate"','id="panchang-request-status"','data-ag74o-r3-request-commit="true"',"AG74O_R3_CALENDAR_ACTIVATION_STYLE_START"])if(!index.includes(marker))fail("Index marker missing: "+marker);
for(const marker of ["drishvaraAg74oApplySelection","SearchRiseSet","SetDeltaTFunction","requestToken","AbortController","final_observance_date_approved===true","public_output_allowed===true","CALENDAR_ACTIVATION_PATH","FESTIVAL_ACTIVATION_PATH","markRequestPending","resolveActivatedCalendarRecord","renderActivatedCalendarRecord",'target.closest("#panchang-calculate")',"computeDay(approvedRequest)"])if(!controller.includes(marker))fail("Controller marker missing: "+marker);
for(const forbidden of ["localStorage.setItem","sessionStorage.setItem",'fetch("https',"fetch('https","supabase","service_role","var LOCATION_MAP = {","var ALIASES = {"])if(controller.includes(forbidden))fail("Forbidden runtime dependency: "+forbidden);
const boot=controller.match(/function boot\(\) \{([\s\S]*?)\n  \}/)?.[1]||"";if(!boot||boot.includes("applySelection("))fail("Boot must not commit or calculate.");
const block=controller.slice(controller.indexOf('  window.addEventListener("change"'),controller.indexOf('  function boot() {'));if((block.match(/applySelection\(/g)||[]).length!==1||!block.includes('target.closest("#panchang-calculate")'))fail("Input handlers must not auto-commit.");
if((index.match(/id=["']panchang-calculate["']/g)||[]).length!==1)fail("Exactly one Calculate Panchang button required.");
if((index.match(/data-ag74i-book-page="[1-4]"/g)||[]).length!==4)fail("Exactly four annual-book pages required.");
if(!/data-ag71e-preview-action-shell="panchang"[^>]*hidden[^>]*aria-hidden="true"[^>]*inert/.test(index))fail("Legacy AG71E panel must remain hidden and inert.");
const vendor=fs.readFileSync("assets/vendor/astronomy-engine-2.1.19.min.js"),source=fs.readFileSync("node_modules/astronomy-engine/astronomy.browser.min.js"),hash=(v)=>crypto.createHash("sha256").update(v).digest("hex");if(hash(vendor)!=="f41139a87941ea017ab902b954c9389fa27ea72083d7fab4971756d7769d14e6"||hash(vendor)!==hash(source))fail("Pinned Astronomy Engine bundle mismatch.");
if(pkg.dependencies?.["astronomy-engine"]!=="2.1.19")fail("Astronomy Engine dependency must remain exact-pinned.");
if(pkg.scripts?.["validate:ag74o-r2"]!=="node scripts/validate-ag74o-r2-selector-calculation-correction.mjs")fail("AG74O-R2 validation wiring must remain intact.");
if(pkg.scripts?.["validate:ag74o-r3"]!=="node scripts/validate-ag74o-r3-governed-calendar-activation.mjs")fail("AG74O-R3 validation wiring mismatch.");
if(runtime.status!=="ag74o_r2_governed_selector_calculation_gate_active_runtime_approval_blocked"||runtime.runtime?.automatic_calculation_on_boot!==false||runtime.runtime?.click_required_for_request!==true||runtime.runtime?.automatic_calculation_on_input_change!==false||runtime.runtime?.public_runtime_activation_allowed_now!==false)fail("Runtime boundary mismatch.");
if(locations.record_count!==0||calendar.approved_daily_record_count!==0||festival.approved_observance_count!==0)fail("Zero-approval activation baseline mismatch.");
pass("AG74O-R2 controls and AG74O-R3 explicit calendar activation flow are statically valid.");
